# Copy to .env and fill every value. The app fails fast at startup if any
# required key is missing — see src/ankura/config.py (Phase 1 Step 3).
#
# Every key below is a placeholder for now; Step 3 defines the real
# pydantic-settings Settings class these must satisfy.

ENV=
DATABASE_URL=
DATABASE_DIRECT_URL=
APP_DB_ROLE=
LOG_LEVEL=
API_KEY_PEPPER=
